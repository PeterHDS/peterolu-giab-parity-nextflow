manifest {
  name            = 'portable-precision-giab-parity'
  nextflowVersion = '>=23.10.0'
}

params {
  reads  = "${baseDir}/../assets/reads/*_{R1,R2}.fastq.gz"
  outdir = "${baseDir}/../results"
}

profiles {
  standard {
    process.executor = 'local'
  }
  docker {
    process.executor = 'local'
    docker.enabled   = true
    docker.runOptions = '-u $(id -u):$(id -g)'
  }
}
